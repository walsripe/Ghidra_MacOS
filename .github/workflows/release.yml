name: Build MacOS APP for Ghidra

on:
  release:
    types: [published]

jobs:
  build-and-upload:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build x86_64
        run: ./build.bash -a x86_64 -f

      - name: Build arm64
        run: ./build.bash -a arm64 -f

      - name: Package DMGs
        run: |
          mkdir -p dist
          hdiutil create dist/Ghidra-x86_64.dmg \
            -volname "Ghidra" \
            -srcfolder Ghidra.app \
            -ov \
            -format UDZO
          hdiutil create dist/Ghidra-arm64.dmg \
            -volname "Ghidra" \
            -srcfolder Ghidra.app \
            -ov \
            -format UDZO

      - name: Upload DMGs to Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/Ghidra-x86_64.dmg
            dist/Ghidra-arm64.dmg
          token: ${{ secrets.GITHUB_TOKEN }}
